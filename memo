배합비 프로그램 만들기 초안

DB = 단가, 원료별 성분표
엑셀에서 성분정보 추출
엑셀로 원료단가 추출

Price, 성분 DB 만들기 완료
DB에서 데이터 추출하기 (단가 추출 완료)
DB에서 일반성분 추출하기

1. input = 원료, 함량
2. output = 전체 배합비, 단가, 일반성분, 아미노산, 지방산 등등
--------------------------------------

일단 입력 및 추출만 진행할 것

1. 만약 DB에 없는 원료를 사용한다면!
2. 입력값이 지정된 문자Type과 다르게 입력됐을때!


그냥 DataFrame째로 가져와서 가공하는 것도 괜찮을듯
table["실제단가"] = table["실제단가"] * table["함량(%)"] / 100